# Server Configuration
server.port=${PORT:8080}
spring.application.name=product-review-backend

# Default Profile - Use railway for Railway deployment
spring.profiles.active=railway

# CORS Configuration
spring.web.cors.allowed-origins=*


